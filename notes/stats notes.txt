- useful data
    - when new tags introduced/reached
    - rate of right/wrong answers per day, in aggregate, per tag, and per path
    - specific paths/tags with most/least wrong answers

- cache common data
    - tags select
    - tags/badges join
    - calendar

- logstats
    - list of log_id's/step/day
    - number of those right and number wrong
    - last wrong (as of that day)
    - number of times right (as of that day)
